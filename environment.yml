name: pph
channels:
  - conda-forge
  - defaults
dependencies:
  - python=3.11
  - geemap
  - geopandas
  - jupyter
  - ruff
  - pip
  - pip:
      - geeft >= 0.1.2
      - geersd >= 0.1.2
      - e .
prefix: C:\Users\rhamilton\AppData\Local\miniconda3\envs\pph
